# Normalize all text files to LF in the repo and on checkout.
# This keeps Prettier (endOfLine: lf) green on Windows checkouts, matching CI.
* text=auto eol=lf

# Binary assets: never touch line endings.
*.pdf binary
